{
  "name": "polygames",
  "version": "0.4.0",
  "license": "",
  "devDependencies": {
    "eslint": "^4.0.0-rc.0",
    "eslint-config-google": "^0.8.0",
    "eslint-plugin-html": "^3.0.0-beta.0"
  },
  "scripts": {
    "lint": "npm run lint:javascript",
    "lint:javascript": "eslint . --ext js,html --ignore-path .gitignore",
    "test": "polymer test",
    "start": "polymer serve",
    "dev": "browser-sync start --server --files \"src/**/*,build/**/*,bower_components/**/*\" --startPath \"/src\" --directory --no-notify",
    "test:integration": "polymer build # test that psk builds without error with the CLI"
  },
  "dependencies": {
    "browser-sync": "^2.18.12"
  }
}